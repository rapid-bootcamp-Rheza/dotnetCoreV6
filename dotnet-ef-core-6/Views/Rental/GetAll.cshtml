@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@model IEnumerable<dotnet_ef_core_6.DataContext.RentalEntity>
@{
    ViewBag.Title = "title";
    Layout = "_Layout";

}

<div>
    <h2>Customer Get All</h2>
    <table class="table table-responsive">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Jenis Kendaraan</th>
                <th>Merk Kendaraan</th>
                <th>Sewa</th>
                <th>HariSewa</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.NamaPenyewa</td>
                    <td>@item.Jenis</td>
                    <td>@item.Merk</td>
                    <td>@item.Sewa</td>
                    <td>@item.HariSewa</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-success">View</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
        <a asp-action="Add" class="btn btn-sm btn-primary">Add</a>
    </table>

</div>
